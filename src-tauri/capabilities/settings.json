{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "The settings window draws its own title bar, so it needs the two things Windows would otherwise provide: a close button and a draggable strip. Nothing else — no filesystem, shell, or network permission is granted here either.",
  "windows": ["settings"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging",
    "core:window:allow-minimize",
    "core:window:allow-unminimize"
  ]
}
